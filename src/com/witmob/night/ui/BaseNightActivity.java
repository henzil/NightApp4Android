package com.witmob.night.ui;import android.content.DialogInterface;import android.os.Bundle;import android.view.KeyEvent;import com.witmob.night.R;import com.witmob.night.ui.widget.LoadingDialog;/** * @author henzil * @version create time:2014-8-31_下午2:42:01 * @Description default base activity */public abstract class BaseNightActivity extends BaseFragmentActivity{	protected LoadingDialog loadingDialog;		@Override	protected void onCreate(Bundle arg0) {		super.onCreate(arg0);		initData();		initViews();		initWidgetActions();	}		//初始化加载对话框	protected void initDialog() {		if (loadingDialog == null) {			loadingDialog = new LoadingDialog(this, R.style.my_dialog);			loadingDialog.setCancelable(true);			loadingDialog.setOnKeyListener(new DialogInterface.OnKeyListener() {				@Override				public boolean onKey(DialogInterface dialog, int keyCode, KeyEvent event) {					if (keyCode == KeyEvent.KEYCODE_BACK) {						if (loadingDialog != null)							loadingDialog.cancel();					}					return true;				}			});			loadingDialog.setCanceledOnTouchOutside(false);		}	}		// 显示加载对话框	protected void showLoadDialog() {		if (loadingDialog != null && !loadingDialog.isShowing()) {			loadingDialog.show();		}	}	// 隐藏加载对话框	protected void hideLoadDialog() {		if (loadingDialog != null && loadingDialog.isShowing()) {			loadingDialog.cancel();		}	}		}